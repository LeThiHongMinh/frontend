import React from 'react';

interface DashboardProps {
    // Define the specific props for your Dashboard component
}

const Dashboard: React.FC<DashboardProps> = (props) => {
    return <h1>Dashboard</h1>;
};

export default Dashboard;
